# 🚀 快速参考卡

## 启动服务器

```bash
# 在项目根目录运行
./start-webapi.sh

# 或手动编译
cd Admin
javac common/*.java
javac -cp . backend/*.java  
javac -cp . webapi/*.java
java -cp . webapi.WebApiServer 8080
```

## 访问应用

```
http://localhost:8080
```

## 测试账户

```
用户名: admin
密码: admin123
```

## API 端点

### 登录
```bash
curl -X POST http://localhost:8080/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 注册
```bash
curl -X POST http://localhost:8080/api/register \
  -H "Content-Type: application/json" \
  -d '{"username":"newuser","password":"password123"}'
```

### 状态检查
```bash
curl http://localhost:8080/api/status
```

## 运行测试

```bash
bash test-webapi.sh
```

## 关键文件

| 文件 | 说明 |
|------|------|
| `web/index.html` | Web 登录页面 |
| `Admin/webapi/WebApiServer.java` | HTTP 服务器 |
| `Admin/backend/UserManager.java` | 用户管理 |
| `start-webapi.sh` | 启动脚本 |
| `WEB_USER_GUIDE.md` | 使用指南 |
| `WEB_TEST_REPORT.md` | 测试报告 |

## 响应格式

**成功响应：**
```json
{
  "success": true,
  "message": "操作成功",
  "data": null
}
```

**失败响应：**
```json
{
  "success": false,
  "message": "错误信息",
  "data": null
}
```

## 常用命令

```bash
# 启动 Web API 服务器
./start-webapi.sh

# 运行测试
bash test-webapi.sh

# 检查服务器状态
curl http://localhost:8080/api/status

# 停止服务器
pkill -f "webapi.WebApiServer"

# 查看服务器日志
cat /tmp/webapi.log

# 在 Admin 目录中编译
cd Admin && javac -cp . webapi/*.java
```

## 故障排除

| 问题 | 解决方案 |
|------|---------|
| 端口已占用 | 更换端口或关闭占用程序 |
| 页面加载失败 | 检查 web/index.html 是否存在 |
| 编译失败 | 确保 Java 已安装并编译所有文件 |
| 无法连接服务器 | 确保服务器已启动 |

## 架构图

```
┌─────────────────┐
│   Web Browser   │
│  (index.html)   │
└────────┬────────┘
         │ HTTP/JSON
         ↓
┌─────────────────────┐
│  WebApiServer:8080  │
│  ├─ POST /login     │
│  ├─ POST /register  │
│  └─ GET /status     │
└────────┬────────────┘
         │ Logic
         ↓
┌─────────────────┐
│  UserManager    │
│  ├─ login()     │
│  ├─ register()  │
│  └─ verify()    │
└────────┬────────┘
         │ Data
         ↓
┌─────────────────┐
│  HashMap<String,│
│     User>       │
└─────────────────┘
```

## 技术栈

- **前端：** HTML5 + CSS3 + JavaScript (Vanilla)
- **后端：** Java 8+ (HttpServer)
- **数据：** 内存存储 (HashMap)
- **通信：** HTTP REST API + JSON
- **部署：** 无依赖，开箱即用

## 功能清单

- ✅ 用户注册
- ✅ 用户登录
- ✅ 服务器状态检查
- ✅ 错误处理
- ✅ 表单验证
- ✅ 响应式设计
- ✅ CORS 支持
- ✅ 内置测试工具

## 下一步

1. 启动服务器
2. 打开浏览器访问 http://localhost:8080
3. 使用 admin/admin123 登录或注册新账户
4. 使用页面下方的快速测试工具进行功能验证

---

**完成时间：** 2026-01-23  
**测试状态：** ✅ 全部通过  
**可部署状态：** ✅ 已准备好
